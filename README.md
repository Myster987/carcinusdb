# CarcinusDB

## Architecture:

1. ### Storage

All data is stored in /carnicusdb directory. Each database has separet folder inside main directory.
Example:

```
/carnicusdb
    - /database1
    - /database2
    - /database3
    ... 
```
